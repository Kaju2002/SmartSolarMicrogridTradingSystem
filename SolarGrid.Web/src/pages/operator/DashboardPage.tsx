import OperatorPlaceholder from './OperatorPlaceholder'

export default function OperatorDashboardPage() {
  return (
    <OperatorPlaceholder
      title="Dashboard"
      subtitle="Overview of your assigned stations and bookings."
      nextStep="Step 5 — KPIs for assigned stations, pending approvals, and today’s activity."
    />
  )
}
