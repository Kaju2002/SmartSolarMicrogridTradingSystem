import OperatorPlaceholder from './OperatorPlaceholder'

export default function OperatorStationsPage() {
  return (
    <OperatorPlaceholder
      title="My Stations"
      subtitle="Solar stations assigned to you."
      nextStep="Step 2 — list stations where you are the assigned operator (view only)."
    />
  )
}
