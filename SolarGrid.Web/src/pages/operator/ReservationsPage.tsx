import OperatorPlaceholder from './OperatorPlaceholder'

export default function OperatorReservationsPage() {
  return (
    <OperatorPlaceholder
      title="Reservations"
      subtitle="Bookings for your stations."
      nextStep="Step 3 — show reservations for your stations and approve pending ones."
    />
  )
}
